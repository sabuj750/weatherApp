<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Weather Result</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <div class="container">
      <% if (city && temp !== 'Not found or error occure') { %>
      <h2>Current temperature in <%= city %> is:</h2>
      <div class="temperature"><%= temp %> °C</div>
      <% } else { %>
      <h2>City not found or error occurred.</h2>
      <% } %>
      <a href="/" class="back-button">Search Again</a>
    </div>
  </body>
</html>
